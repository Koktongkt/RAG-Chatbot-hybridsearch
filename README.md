This project implements a local Retrieval-Augmented Generation (RAG) pipeline using:

Document → original documents (pdf, word docs, txt, etc)
ChromaDB → vector storage
LangChain → orchestration
MXBAI embeddings → document vectorization
Local LLM using Ollama/gemma4:26b → response generation
Web frontend → chatbot interface

